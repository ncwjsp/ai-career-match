# Local development only. Copy to .env; never use these passwords in deployment.
POSTGRES_ADMIN_PASSWORD=local_admin_only
APP_DB_PASSWORD=local_app_only
JOB_DB_PASSWORD=local_jobs_only
POSTGRES_PORT=5432
